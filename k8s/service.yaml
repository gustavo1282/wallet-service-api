apiVersion: v1
kind: Service
metadata:
  name: wallet-service
spec:
  type: NodePort
  selector:
    app: wallet
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30001