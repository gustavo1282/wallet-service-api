server:
  protocol-type: http
  hostname: localhost
  port: '8080'
  servlet:
    context-path: /wallet-service-api
controller:
  path:
    base: "/api"
    #base-mapping: "/api"
    #full: "/wallet-service-api/api"

wallet-service-api:
  seed-path: classpath:./data/seed/

springdoc:
  packagesToScan: com.guga.walletserviceapi   # ou seu pacote base exato
  pathsToMatch: /api/** 
  api-docs:
    #server-url: http://localhost:8080/wallet-service-api/api
    path: /v3/api-docs
    version: OPENAPI_3_0
    enabled: true
  info:
    title: Microservices Wallet Service
    description: API REST manages the entire lifecycle of a wallet service
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: false  
    try-it-out-enabled: 'false'
    groups-order: ASC

spring:
  application:
    name: wallet-service-api
  cloud:
    vault:
      enabled: false
    compatibility-verifier:
      enabled: false
  data:
    web:
      pageable:
        default-page-size: 25
        max-page-size: 10
        page-parameter: page
        size-parameter: size
  ####
  # CONFIGURACAO LOCAL UTILIZANDO H2
  ####
  datasource:
    #url: jdbc:h2:mem:db_wallet;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    #jdbc:h2:file:~/dados/db_wallet;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
    url: jdbc:h2:file:./target/test-data/wallet_db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: wallet_user
    password: wallet_pass
  # Configurações do console H2
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
  jpa:
    hibernate:
      ddl-auto: create-drop
      use_sql_comments: true
      #enable_lazy_load_no_trans: true
    generate-ddl: true
    properties:
      #jakarta.persistence.sql-load-script-source: classpath:data.sql
      #hibernate.hbm2ddl.delimiter: ','
      jdbc.batch_size: 1000
      #jakarta.persistence.schema-generation.scripts.action: drop-and-create
      #jakarta.persistence.schema-generation.create-source: metadata
      #jakarta.persistence.schema-generation.scripts.create-target: ./data/schema-create.sql
      #jakarta.persistence.schema-generation.scripts.drop-target: ./data/schema-drop.sql
      #jakarta.persistence.schema-generation.scripts.target: file, console
    show-sql: 'true'
    format_sql: true
  jackson:
    default-property-inclusion: non_null
    #property-naming-strategy: SNAKE_CASE
    property-naming-strategy: LOWER_CAMEL_CASE
    deserialization:
      fail-on-unknown-properties: false
    #serialization:
    #  fail-on-empty-beans: false
  #sql:
    #init:
      #mode: always
      #defer-datasource-initialization: true      
      #data-locations:
      #- classpath:data.sql
  security:
    user:
      name: "wallet_user"
      password: "wallet_pass"
      roles: "MONITOR"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    prometheus:
      enabled: true
    metrics:
      enabled: true
    health:
      show-details: always

logging:
  level:
    # Nível mais alto (TRACE/DEBUG) para detalhar a causa raiz do erro de persistência
    # (Este é o foco para debugar o erro de JPA/Hibernate)
    org.hibernate.engine.jdbc.spi: warn 
    org.hibernate.orm.jdbc.batch: warn # Detalhes sobre a execução do batch JDBC
    org.hibernate.SQL: warn          # Mostra as queries SQL geradas

    # Nível DEBUG para a camada de transação do Spring e JPA
    org.springframework.orm.jpa: warn
    org.springframework.transaction: warn
    org.springframework.security: warn # Mantém a segurança em INFO (nível padrão para evitar flood)

jwt:
  secret: 'CHAVE_SECRETA_MOCK_PARA_TESTES_DE_INTEGRACAO'
  # Você pode usar a mesma frase do seu .env aqui, se quiser consistência.