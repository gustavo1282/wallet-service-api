# act -P ubuntu-latest=ghcr.io/catthehacker/ubuntu:full-latest
# act -j sonar
# act -j build_test
# act -j build_test   
# if: false     # pular coisas específicas
# if: github.event_name == 'workflow_dispatch'    # criar modo debug

name: Pipeline > wallet-service-api

on:
  push:
    branches: [main, develop, "feature/*"]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_test:
    name: Build & Test (Java 21)
    runs-on: ubuntu-latest

    outputs:
      build-done: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: maven

      - name: Make mvnw executable
        run: chmod +x ./mvnw

      # 🔥 REMOVIDO clean → ganho de tempo
      - name: Maven Verify (tests + checks)
        run: |
          ./mvnw -B -T 1C verify \
            -DfailIfNoTests=false \
            -Dsurefire.useFile=false \
            -Dfailsafe.useFile=false

      # 🔥 MANTIDO, mas só se necessário (ex: debug ou histórico)
      - name: Upload JaCoCo report (artifact)
        if: false  # 👈 desativado por padrão (remove overhead)
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: target/site/jacoco/
          retention-days: 1

  sonar:
    name: SonarQube / SonarCloud
    runs-on: ubuntu-latest
    needs: [build_test]

    steps:
      - name: Checkout (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: maven

      - name: Make mvnw executable
        run: chmod +x ./mvnw

      # 🔥 REMOVIDO download de artifact (não precisamos mais)

      - name: Cache Sonar
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      # 🔥 NÃO rebuilda tudo → usa skipTests e evita custo pesado
      - name: Analyze (Sonar)
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./mvnw -B -T 1C compile sonar:sonar \
            -DskipTests \
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }} \
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }} \
            -Dsonar.host.url=${{ secrets.SONAR_HOST_URL }} \
            -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml

  docker:
    name: Docker Build & Push
    runs-on: ubuntu-latest
    needs: [build_test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set Docker metadata (tags)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/wallet-service-api
          tags: |
            type=raw,value=latest
            type=sha,format=short
            type=raw,value=build-${{ github.run_number }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
